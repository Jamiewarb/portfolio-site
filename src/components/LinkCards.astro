---
type Props = astroHTML.JSX.HTMLAttributes;

const { class: className, ...rest } = Astro.props;
---

<div class:list={['link-cards', className]} {...rest}>
  <slot />
</div>

<style>
  .link-cards {
    display: grid;
    gap: 1.5rem;
    margin-top: 2.5rem;
  }
</style>
